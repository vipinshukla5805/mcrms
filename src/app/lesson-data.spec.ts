/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {LessonData} from './lesson-data';

describe('LessonData', () => {
  it('should create an instance', () => {
    expect(new LessonData()).toBeTruthy();
  });
});
